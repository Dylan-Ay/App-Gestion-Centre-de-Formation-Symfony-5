{% extends 'base.html.twig' %}

{% block title %}Gestion des modules{% endblock %}

{% block body %}

<h1>Gestion des modules</h1>
<section id="modules-list" class="py-5 d-flex justify-content-evenly flex-wrap gap-5">

    <table class="table table-hover mt-4">
        <thead>
          <tr>
            <th class="text-center">Développement</th>
          </tr>
        </thead>
        <tbody>

        <tr class="table-active d-flex flex-column">
            {% for module in modulesList %}
                <td class="d-flex justify-content-between">
                    {{module.title}} 

                    <div class="btn-container">
                        <a href="#" class="btn btn-success">Editer</a> 
                        <a href="#" class="btn btn-danger">Supprimer</a>
                    </div>
                    
                </td>
            {% endfor %}
        </tr>
        </tbody>
      </table>

</section>

{% endblock %}